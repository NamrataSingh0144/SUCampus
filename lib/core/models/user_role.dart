/// Defines the possible roles a user can have within the application.
/// Using an enum prevents typos and makes the code more readable.
enum UserRole {
  student,
  shop, // Represents Restaurant/Shop Owner
  admin,
}